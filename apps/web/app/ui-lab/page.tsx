import { UiLab } from "../../components/ui-lab";

export const metadata = {
  title: "UI laboratory | Supademo",
  robots: { index: false, follow: false }
};

export default function UiLabPage() {
  return <UiLab />;
}
